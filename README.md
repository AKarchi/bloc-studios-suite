This is the link for the summary or information website, a landing page like.
https://bloc-studios-suite.vercel.app/

This is the link for the actual web platform. No sign up/sign in. Ad-free for both pro access and free access. Yes, it's a freemium, but it is a lifetime access, one-time payment only at minimum of $3 via Ko-fi. 

https://capable-smakager-c0cec3.netlify.app/

🔒 **DATA PRIVACY & AUTO-SAVE NOTE**
**How Auto-Save works:**  Every letter you draft, every task you toggle, and every schedule block you modify triggers an instantaneous mechanical auto-save directly to your browser's local sandbox memory (`LocalStorage`). There are no "Save" buttons because your work is committed on every single keystroke.

**Absolute Isolation:**  This application is completely local-first. We do not use external cloud servers or database tracking systems. Because your data lives strictly inside your physical machine, **no other person on earth can read, view, or open your files—even if they visit the exact same website URL** . Your workspace is structurally invisible to everyone but you.

**Device & Browser Boundaries:**  Because data is saved locally inside the browser app itself, **it will not sync automatically across devices.** If you open this website on another device (like your phone) or a different browser (like switching from Chrome to Safari), your data will look completely empty. Google Account Sync does not sync website data.

🔄 **Moving Your Data:** If you need to switch from your laptop to your phone, or want to create a hard copy of your files, use our built-in **Export JSON** button to download a file, send it to your other device, and hit **Import JSON**.


⚠️ **CRITICAL WARNING: BROWSER CACHE & DATA CLEARING**

**Bloc Studio runs entirely on local execution with no external cloud databases.** All your data is saved strictly inside your browser's local sandbox data structure (`localStorage`).

If you clear your browser cache, browsing data, or site cookies:

* ❌ **Wipe Tasks & Project Planners:** Your active task grid and scheduling logs (`bloc_todos`, `bloc_scheduler`) will be completely deleted.


* ❌ **Lose Document Registries:** Any unexported text assets, records, and quick-save data slots (`bloc_notes`, `bloc_archive_log`) will be permanently lost.


* ❌ **Reset Telemetry Sheets & Sandboxes:** Daily habit metrics (`bloc_caffeine`, `bloc_hydration`, `bloc_radar`) as well as temporary buffers like the Shredder matrix or Hex-Vault configurations will return to standard factory configurations.


* ❌ **Revoke Premium License Keys:** Your local Pro-Rig activation flag (`bloc_premium`) will be removed, requiring you to re-authenticate or re-enter your access parameter.



 **🛠️ How to Prevent Unintended Data Loss:**

Before performing any system maintenance or cleaning your browser environments, use the built-in data pipeline:

1. **Export Dynamic Backups Regularly:** Navigate to the Data Engine and invoke **Export JSON**  (or use the designated backup control) to compile your entire ecosystem matrix into a clean `.json` file downloaded straight to your local drive.


2. **Re-establish Your Local Workspaces:** If a wipe occurs, you can fully restore your environment by running **Import JSON** to parse your saved `.json` matrix directly back into the engine configuration blocks.

**NOTE: NO DATA IS BEING COLLECTED EVER**

- Bloc. Studios | MochiiWorks 
